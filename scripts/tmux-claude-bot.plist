<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.octopusgarage.tmux-claude-bot</string>

  <key>ProgramArguments</key>
  <array>
    <string>__PROJECT_DIR__/scripts/__WRAPPER__</string>
  </array>

  <key>WorkingDirectory</key>
  <string>__PROJECT_DIR__</string>

  <key>RunAtLoad</key>
  <false/>

  <key>KeepAlive</key>
  <true/>

  <key>ThrottleInterval</key>
  <integer>10</integer>

  <key>StandardOutPath</key>
  <string>__PROJECT_DIR__/logs/launchd.out.log</string>

  <key>StandardErrorPath</key>
  <string>__PROJECT_DIR__/logs/launchd.err.log</string>

  <!--
    Only PATH is set here. The wrapper first removes the closed set of sanitized
    inherited credential-shaped names, then selects the trusted state .env.
    That explicit file is loaded by dotenv with override semantics, so managed
    service configuration wins over remaining inherited values. Do not duplicate
    managed configuration in launchd EnvironmentVariables.
  -->
  <key>EnvironmentVariables</key>
  <dict>
    <key>PATH</key>
    <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
  </dict>
</dict>
</plist>
