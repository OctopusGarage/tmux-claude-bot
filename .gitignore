node_modules/
dist/
.venv/
.env
.current_project
recent_projects.txt
session_path_map.json
group_bindings.json
workspaces.json
media-cache/
coverage/
.queue/
.playwright-cli/
logs/
!src/core/logs/
.bot.pid
# Ignore local Claude settings, but track shared slash commands and the
# shared AI-agent quality-gate hooks (settings.local.json stays ignored).
.claude/*
!.claude/commands/
!.claude/settings.json
# Project-shared agent automation (the /audit engine + its methodology).
!.claude/workflows/
!.claude/skills/
docs/superpowers/
.running

# Live runtime state belongs to the user state directory or a dedicated private
# backup, never in this source repository.
/state/
/state/resource-guardian/

# Stryker mutation testing artifacts
reports/
.stryker-tmp/
.superpowers/
.codegraph/
# Personal, machine-specific operating rules (self-evolving dev mode); not shared.
/CLAUDE.local.md

# added by git-auto-sync
state/.env.bak.1782450641
state/.env.bak.loop-1784174356044
