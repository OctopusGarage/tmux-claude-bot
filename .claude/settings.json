{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); [ -z \"$f\" ] && exit 0; npx @biomejs/biome check \"$f\" --no-errors-on-unmatched 1>&2 || exit 2"
          }
        ]
      }
    ]
  }
}
