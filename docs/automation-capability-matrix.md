# Automation Capability Matrix

This matrix tracks which user/control surfaces expose each intelligent
automation capability. It is a maintenance aid for avoiding Telegram/Feishu/TUI
or CLI drift.

Authoritative behavior lives in source and tests. When this matrix disagrees
with source, fix the source/docs alignment in the same slice.

## Surfaces

| Surface | Role |
| --- | --- |
| CLI | Local operator/admin surface through `tcb ...`. |
| Telegram | Owner chat surface with commands and inline keyboards. |
| Feishu/Lark | Owner chat plus project-bound group surface with commands and cards. |
| TUI | Local terminal client over the control socket. |
| Home/operator skill | AI-facing recipe for operating `tcb` from Claude Code/Codex. |
| MCP | Structured AI tool surface; read-only Observer profile via `tcb mcp observer` and controlled Home profile via `tcb mcp home`. |

## Capability Matrix

| Capability | CLI | Telegram | Feishu/Lark | TUI | Home/operator skill | MCP | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Ordinary project prompt | `tcb send <project> ...` | Send text/voice in current project chat | Send text/voice in private chat or bound project group | Compose prompt | Describes `tcb send` | `tcb.home.send_prompt` | Must block when active automation owns the project unless using diagnostic/escape controls. |
| Project/session intake and recovery | `tcb projects/open/open-worker/adopt/recover` | Project browser, add project, independent session, adopt, and confirmed host-wide recovery | Project/group bind and creation, independent session, adopt, and confirmed host-wide recovery | `s` project browser and `R` recovery | Describes discovery/open/recovery and target selection | Observer projects/sessions are read-only; Home has no intake mutation tool | Paths and unmanaged processes require validation and confirmation. Worker opening is an automation boundary and stays CLI/control-only. |
| Session control and evidence | `tcb sessions/peek/control/logs`, plus send and attachment drilldowns | Control panel, history, inputs, resumable sessions, queue, peek, and logs | Equivalent command/card intent with project-group boundaries | Live pane, logs, inputs, controls, and attach-to-pane | Uses typed MCP first, then bounded CLI drilldowns | Observer sessions/queue/logs; Home adds explicit-session prompt delivery | Shared core action policy owns queueing, confirmation, and preconditions; adapters do not invent control semantics. |
| Voice transcription | Optional install/status via setup, doctor, and install scripts; no separate send command | Voice message handling, `/voice_install`, `/voice_lang` | Audio resource handling, `/voice_lang`, `voiceinstall` card action | Not directly surfaced | Describes voice setup and prompt delivery | Not exposed | Input enhancement only: transcribed text enters the same queue as owner text and keeps voice provenance. |
| Prompt translation | `tcb prompt-translate status\|off\|on [from] [to]`, plus setup/doctor support | `/prompt_translate`, `/translate_install`, prompt-translation keyboard | `/prompt_translate`, `/translate_install`, prompt-translation cards | `T` toggles local control translation off or Argos zh→en | Describes prompt translation setup | Not exposed | Applies before enqueue for text or transcribed voice; failure blocks delivery instead of silently changing intent. |
| Runtime Overview and session status | `tcb dashboard` / `--json` is the full local/admin view; project/problem filters apply to both formats; `tcb sessions`, `tcb peek`, `tcb logs`, and `tcb runtime-guardian findings --project <id> --limit 20` are drilldowns | `/dashboard` uses the same bounded health-first snapshot, summarizes healthy/idle detail, and keeps attention explicit; `/peek`, `/history`, and `/logs` drill down | Same Runtime Overview, bounded summaries, and drilldown command intent through Lark handlers/cards | Header summary plus `o` for the read-only Runtime Overview; rows are health-first with identity-stable selection; live pane and logs drill down | MCP-first `tcb.observer.status`; `tcb dashboard --json` fallback | `tcb.observer.status`, `tcb.observer.projects`, `tcb.observer.sessions`, `tcb.observer.queue`, `tcb.observer.logs_query`, `tcb.observer.loop_reports_list`, `tcb.observer.daily_task_audit`, and `tcb.observer.runtime_guardian_findings` expose typed read-only snapshots and bounded Control-backed drilldowns | All entry points project one neutral snapshot, including Agent Capacity and its bounded attention state. Collector timeout/read failures degrade only their domain with stable error kinds; no overview surface mutates state. |
| Autopilot active delegation | `tcb autopilot <project> [requirement]` | `/autopilot`, inline **Delegate now**, **Review plan first** then **Confirm delegation**, and queue view on blocked delegation | `/autopilot`, cards **Delegate now**, **Review plan first**, queue view, and cancellable active-delegate queue rows | `A` panel delegates via supervisor | Describes `tcb autopilot` | `tcb.home.delegate_autopilot` | Current Autopilot is supervisor-backed delegation only. No keep-alive, goal-cycle, enable/disable, or old human gate UI. Queue cancellation is limited to active-delegated tasks, not scheduled system WorkOrders. |
| Opportunity Discovery suggestions | Inspect via stored reports/commands; no direct top-level CLI command beyond related loop reports | `/opportunity list/show/discuss/dismiss/snooze`, per-suggestion keyboard when callback payloads fit Telegram limits; typed-command fallback for oversized callback ids | `/opportunity list/show/discuss/dismiss/snooze`, readable per-suggestion cards with view/discuss/dismiss actions plus batch actions | Not directly surfaced | Describes `/opportunity` flow | Not exposed | Discovery is read-only. Snooze removes an item from the active numbered list for 14 days. Discussion and execution are decoupled; approved work enters Autopilot delegation. |
| Prompt Library | No dedicated top-level browser; setup/doctor report configured readiness | `/prompts` search, tag, page, inspect, and send flow | `/prompts` cards with the same browse/send intent | Not directly surfaced | Routes to the maintained chat workflow | Not exposed | External Prompt Library content is user-managed input, not governed system-prompt truth. Disabled/error states are explicit. |
| Personal configuration admin | `tcb config list/get/set`; secrets redacted, generic writes allowlisted and domain-validated | Setup/settings commands for supported chat preferences | Setup/settings commands for supported chat preferences | Limited preference toggles | Describes setup and config inspection commands | Not exposed | Secret-bearing credentials stay behind setup/dedicated flows, not generic `config set`; routine CLI, automation, setup/onboarding, and chat-preference `.env` mutations share one cross-process writer. |
| Background automation master switch | `tcb automation status/pause/resume <loop\|task-audit\|runtime-guardian>` | Not directly surfaced | Not directly surfaced | Not directly surfaced | Describes automation pause/resume commands | Not exposed | Pauses high-cost loops by changing their env-backed tick/enabled controls and records previous values for resume. |
| Host power and quiet hours | `tcb power status`, bounded `tcb power history`, `tcb power schedule install/remove`, and allowlisted `TCB_KEEP_AWAKE_MODE` config | No direct control; delivery while asleep follows provider behavior | No direct control; events while asleep are best-effort and may be missed | Runtime Overview reports the Power domain; no mutation | Describes the scheduled setup and diagnostics | `tcb.observer.status` reports the read-only Power domain | macOS owns sleep. Interactive local CLI is the only wake-schedule mutation path; no chat/MCP wake or force-sleep control exists. History correlates only the newest quiet-window cycle in the requested lookback. |
| Agent capacity and autonomous admission | `tcb automation capacity status/history` provides the full bounded local audit view | Runtime Overview plus durable exhaustion/recovery transition alerts | Runtime Overview plus durable exhaustion/recovery transition alerts | Runtime Overview Agent Capacity domain only | Uses Observer status first and the local CLI for bounded history | `tcb.observer.status` reports the current read-only Agent Capacity domain | Durable events record planned, superseded, deferred, admitted, settled, and capacity-transition decisions without raw provider output, prompts, credentials, or paths. Identical capacity state is silent across restart; mutation and quota bypass are not exposed. |
| Home Operator and AI tool installation | `tcb ai-tools install/status`, `tcb skill install/status/uninstall`, `tcb mcp observer/home/install` | `/home` selects the managed operator session | `/home` selects the managed operator session | Home session is visible but installation stays CLI-only | Bundled `tcb-home-operator` recipe; managed install defaults to `<state-dir>/home` | Observer is read-only; Home inherits Observer and adds explicit-session prompt/delegation only | Installation scope is not mutation authority. Global skill publication is explicit opt-in; MCP never exposes arbitrary shell, direct edits, or PR merge. |
| Governed prompts and capability dependencies | `tcb prompts governed list/show/render/check/eval`, `tcb capabilities list/status/install/update`, `tcb loop skills list/sync/refresh` | No admin surface | No admin surface | No admin surface | Points to prompt/capability maintenance docs | Not exposed | Prompt eval emits an active-agent task; it never calls a model provider. External skills are curated/pinned and are not silently installed. |
| Attachments and outbound notifications | `tcb attach`, `tcb notify`, and `tcb task report` | Validated inbound media, chat-bound replies, and owner notifications | Validated inbound media, project-group replies, and owner/project-bound notifications | No file picker; attached session remains available | Describes `attach`, notification attachments, and task reporting | Not exposed | Local paths, size/type limits, and reply targets are validated. Project-scoped Lark delivery carries session identity; generic MCP file access is intentionally absent. |
| Setup, service lifecycle, and diagnostics | `tcb setup/setup:lark/install/service/doctor`; managed install refreshes operator AI surfaces | `/doctor` and optional dependency setup commands; no service mutation | `/doctor`, onboarding, and optional dependency setup; no service mutation | Connection/health visibility only | Uses local CLI for install, restart, and diagnosis | Runtime Overview exposes bounded service/interface evidence; no lifecycle mutation | Host/service/install changes stay local. Chat diagnostics are redacted, and MCP must not become a remote process manager. |
| Loop Engineering admin | `tcb loop validate/tick/run/reports/backlog/skills/targets`, `tcb automation status/pause/resume loop`, `tcb capabilities list/status/install/update` | Primarily via notifications and commands described in docs | Primarily via notifications and commands/cards described in docs | Not a full admin surface | Describes loop admin and capability dependency commands | `tcb.observer.loop_reports_list` only | Agent-supervised manual `loop run` is intentionally blocked; managed scheduler/supervisor owns it. Capability commands expose curated external skill/tool dependencies; `loop skills` remains the registry sync path. |
| Automation Governance Review | Configured under Loop Engineering `automationGovernanceReview` | Result notifications | Result notifications | Not directly surfaced | Points to loop docs | Not exposed | Reviews tmux-claude-bot automation governance; repair PRs are P0/P1-only and must not auto-merge. |
| Daily Task Audit | `tcb task audit --force`, `tcb task report ...`, `tcb automation status/pause/resume task-audit` | Runtime Overview plus actionable unresolved-issue alerts; all-clear is silent | Same, with project-bound routing when applicable | Runtime Overview summary; detailed audit control is intentionally CLI/MCP-only | Reads the Runtime Overview first, then uses dedicated audit/report commands | `tcb.observer.status` summary and `tcb.observer.daily_task_audit` drilldown | Audit actively discovers bot-hosted schedules and can dispatch repair when enabled. Healthy/autoclosed runs stay observable without proactive chat noise; MCP observes persisted ledger state only. |
| Runtime Guardian | `tcb dashboard`, `tcb runtime-guardian findings [--project <id>] [--limit <n>]`, `tcb automation status/pause/resume runtime-guardian`, logs, and MCP observation | Runtime Overview summary plus notification/audit evidence when it dispatches or blocks; dashboard attention points to the local findings drilldown, not repair buttons | Same Runtime Overview summary and evidence; dashboard attention points to the local findings drilldown, not repair buttons | Runtime Overview summary; repair controls are intentionally not exposed | Reads the Runtime Overview first, then maintained diagnostics | `tcb.observer.status` summary and `tcb.observer.runtime_guardian_findings` drilldown | Repairs tmux-claude-bot runtime only, not target projects; CLI/MCP findings drilldowns discover findings without dispatching repair. |
| Resource Guardian | `tcb resource status\|incidents\|mode\|profile`; generic config may set enabled/tick only | Existing `/sysload` shows current Guardian view; no new command or button | Existing `/sysload` card shows current Guardian view; no new command or card action | Existing sysload diagnostic through the control client includes the current Resource Guardian view | Describes safe rollout and read-only incident inspection | Not exposed | Protect refuses while disabled. Mode/profile are dedicated local commands; incidents are bounded and output is secret-free and tildeified. |
| Project/workspace PR review | Configured under Loop Engineering `pullRequestReview` | Result notifications | Result notifications | Not directly surfaced | Points to loop docs | Not exposed | Reviews loop-created PRs for configured project/workspace. |
| Repository-wide PR review | Configured under `prReview.repositories` | Result notifications | Result notifications | Not directly surfaced | Points to loop docs | Not exposed | Inspects every open PR, including Draft/conflicting PRs; may repair narrow same-repo issues, mark ready, merge, or close with evidence. |

## Intentional Differences

- Feishu/Lark supports project-bound groups. Telegram is owner-directed unless a
  feature explicitly configures a project chat target.
- Feishu/Lark uses cards for richer actions. Telegram may use inline keyboards or
  concise text commands. The requirement is capability parity, not identical UI.
- The TUI is a local control client, not a full scheduler administration UI.
- The home/operator skill should stay short and route to `docs/agents/usage-guide.md`
  for detailed recipes.
- Local host, credential, install-scope, plan-file, and destructive administration
  stays in CLI even when read-only health is visible through chat, TUI, or MCP.
- MCP is a typed AI boundary, not a mirror of every CLI command. Observer remains
  read-only; Home adds only target-explicit operations backed by existing gates.
- Capabilities outside intelligent automation, such as ordinary navigation and
  low-level session keys, remain canonical in `docs/commands.md` and `docs/tui.md`
  instead of inflating this matrix.

## Alignment Checklist

When changing a capability above:

1. Update the source handler/control path.
2. Update or intentionally decline Telegram, Feishu/Lark, TUI, CLI, and skill
   support.
3. Record the intentional difference in this file.
4. Update `docs/manual.md`, `docs/commands.md`, `docs/tui.md`,
   `docs/ai-tool-surface-governance.md`, or
   `docs/agents/usage-guide.md` as applicable.
5. Add or update tests for any mechanical behavior that can drift.
